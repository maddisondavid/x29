# Assumptions

- Teams prefer markdown files over chat-only state.
  Impact if wrong: the toolkit will need alternate collaboration surfaces.

- Internal reviewers are willing to annotate with `human:` markup.
  Impact if wrong: review guidance must be adapted.
